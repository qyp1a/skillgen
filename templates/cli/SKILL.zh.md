---
name: {{name}}
description: "{{description}}"
---

# {{name_title}}

> CLI 封装型技能 · 由 skillgen 生成

## 概述
<!-- 说明这个技能封装了哪个命令行工具、解锁了什么工作流。 -->

## 前置条件
<!-- 开始前先验证工具存在；列出版本检查和权限要求。 -->
- 已安装 `xxx` CLI。验证命令：`xxx --version`
- ...（权限、API 密钥、网络访问）

## 命令速查
<!-- 只记录 Agent 实际会用到的子命令 / 参数。 -->

| 命令 | 用途 | 示例 |
| --- | --- | --- |
| `xxx &lt;子命令&gt;` | ... | `xxx &lt;子命令&gt; --flag` |

## 标准工作流
1. 验证工具可用：`xxx --version`
2. 执行主命令：...
3. 检查输出（见"输出解析"）。
4. 如有错误则处理（见"故障处理"）。

## 输出解析
<!-- 退出码、stdout/stderr 约定、JSON 或文本输出。 -->
- 退出码 `0` → 成功；非 0 → 见"故障处理"。
- ...

## 示例

### 示例 1
**用户：** ...
**命令：** `xxx ...`
**预期输出：** ...

## 故障处理

| 现象 | 可能原因 | 恢复方法 |
| --- | --- | --- |
| ... | ... | ... |
