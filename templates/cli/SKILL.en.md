---
name: {{name}}
description: "{{description}}"
---

# {{name_title}}

> A CLI-wrapping skill · Generated by skillgen

## Overview
<!-- What CLI tool this skill wraps and what workflows it enables. -->

## Prerequisites
<!-- Verify the tool exists before starting; list version checks and permissions. -->
- `xxx` CLI installed. Verify with: `xxx --version`
- ... (permissions, API keys, network access)

## Tool Reference
<!-- Document only the subcommands / flags the agent will actually use. -->

| Command | Purpose | Example |
| --- | --- | --- |
| `xxx <sub>` | ... | `xxx <sub> --flag` |

## Standard Workflow
1. Verify tool availability: `xxx --version`
2. Run the primary command: ...
3. Inspect output (see Output Parsing below).
4. Handle errors if any (see Failure Handling).

## Output Parsing
<!-- Exit codes, stdout/stderr conventions, JSON vs text output. -->
- Exit code `0` → success; non-zero → see Failure Handling.
- ...

## Examples

### Example 1
**User:** ...
**Command:** `xxx ...`
**Expected output:** ...

## Failure Handling

| Symptom | Likely cause | Recovery |
| --- | --- | --- |
| ... | ... | ... |
