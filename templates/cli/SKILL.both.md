---
name: {{name}}
description: "{{description}}"
---

# {{name_title}}

> A CLI-wrapping skill / CLI 封装型技能 · Generated by skillgen / 由 skillgen 生成

## Overview / 概述
<!-- What CLI tool is wrapped and what it enables / 封装了什么 CLI、解锁了什么 -->

## Prerequisites / 前置条件
<!-- Verify before starting / 开始前先验证 -->
- `xxx` CLI installed. Verify / 已安装并验证：`xxx --version`
- ... (permissions, keys / 权限、密钥)

## Tool Reference / 命令速查

| Command / 命令 | Purpose / 用途 | Example / 示例 |
| --- | --- | --- |
| `xxx <sub>` | ... | `xxx <sub> --flag` |

## Standard Workflow / 标准工作流
1. Verify / 验证：`xxx --version`
2. Run / 执行：...
3. Inspect output / 检查输出.
4. Handle errors / 处理错误.

## Output Parsing / 输出解析
<!-- Exit codes, stdout/stderr conventions / 退出码、输出约定 -->
- Exit code / 退出码 `0` → success / 成功；non-zero / 非 0 → Failure Handling / 见故障处理

## Examples / 示例

### Example 1 / 示例 1
**User / 用户：** ...
**Command / 命令：** `xxx ...`
**Expected output / 预期输出：** ...

## Failure Handling / 故障处理

| Symptom / 现象 | Likely cause / 可能原因 | Recovery / 恢复方法 |
| --- | --- | --- |
| ... | ... | ... |
