# Node CLI Regression Test

A complete example skill generated in the style of skillgen output. See `SKILL.md` for the skill definition and `scripts/run-cli-cases.mjs` for the deterministic runner.

## Install

For Codex repository use, place this folder at `.agents/skills/node-cli-regression-test`. For personal use, place it at `$HOME/.agents/skills/node-cli-regression-test`.

## License

MIT © Your Name
